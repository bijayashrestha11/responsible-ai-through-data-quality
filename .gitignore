# Python
.venv/
.venv-gate/
__pycache__/
*.pyc

# Cached benchmark intermediates (regenerated by experiment/run_all.py)
results/cache/

# LaTeX build artifacts (paper compiles on arXiv/Overleaf; keep source only)
*.aux
*.bbl
*.blg
*.log
*.out
*.synctex.gz
paper/*.pdf

# OS
.DS_Store

# Claims/eligibility data — NEVER commit raw data or identifiers (CLAUDE.md privacy rule)
experiment/claims/data/
*.parquet

# Internal strategy / working docs — kept local-only (competitive positioning).
# Remove these two lines if/when you decide to publish them (e.g. alongside the preprint).
research-brief.md
CLAUDE.md
